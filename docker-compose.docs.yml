services:
  docs:
    image: task_manager_local_docs
    container_name: task_manager_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./task_manager:/app/task_manager:z
    ports:
      - '9000:9000'
    command: /start-docs
